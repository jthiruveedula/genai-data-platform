import type { CloudId } from "../modules";
import type { FlavorEntry } from "./gcp";

export const cloud: CloudId = "oss";

export const flavor: Record<string, FlavorEntry> = {
  "00-foundations": {
    services: ["vLLM", "HF Hub"],
    storage: "N/A (concepts only)",
    snippet: `# No infra calls in this module — pure concepts.\n# Hugging Face Hub is where you'll pick embedding + LLM models later.`,
    labSteps: [
      "Read the concept page and glossary.",
      "Browse the Hugging Face Hub model catalog (no downloads yet).",
    ],
    costNote: "$0 — no infrastructure created in this module.",
    claimId: "oss-hf-hub-catalog",
  },
  "10-ingestion": {
    services: ["MinIO", "Airflow", "Postgres"],
    storage: "MinIO (raw) + Postgres (structured)",
    snippet: `mc mb local/gdp-raw\n\nmc cp ./sample-docs/*.pdf local/gdp-raw/\n\n# Airflow DAG lands metadata rows in Postgres\nairflow dags trigger gdp_ingest_docs`,
    labSteps: [
      "Create a MinIO bucket for raw documents (docker compose up minio).",
      "Upload 3-5 sample PDFs with the mc client.",
      "Trigger the Airflow DAG to land metadata rows in Postgres.",
      "Verify row count in Postgres matches uploaded file count.",
    ],
    costNote: "$0 on a local kind cluster; ~compute cost only if run on cloud VMs.",
    claimId: "oss-airflow-ingestion",
  },
  "15-chunking": {
    services: ["Airflow", "Postgres"],
    storage: "Postgres (chunked rows with metadata)",
    snippet: `# Recursive chunking with LangChain, run as an Airflow task\nfrom langchain_text_splitters import RecursiveCharacterTextSplitter\n\nsplitter = RecursiveCharacterTextSplitter(chunk_size=800, chunk_overlap=120)\nchunks = splitter.split_text(document_text)\n\n# Each chunk row: (doc_id, chunk_id, text, char_start, char_end)\n# inserted into the chunks table via psycopg2`,
    labSteps: [
      "Take the raw rows landed in Module 10's Postgres table.",
      "Run the recursive chunker at chunk_size=800, chunk_overlap=120 as an Airflow task.",
      "Insert chunk rows (doc_id, chunk_id, text, offsets) into a new chunks table.",
      "Re-run with chunk_size=200 and compare: count chunks, spot orphaned mid-sentence splits.",
    ],
    costNote: "$0 on a local kind cluster; negligible CPU for a handful of documents.",
    claimId: "oss-airflow-chunking",
  },
  "20-embeddings": {
    services: ["TEI (Text Embeddings Inference)", "Qdrant"],
    storage: "Qdrant collection",
    snippet: `# Embed via a local TEI server (bge-base-en-v1.5)\nresp = requests.post("http://tei:80/embed", json={"inputs": [c.text for c in chunks]})\nvectors = resp.json()\n\nclient.upsert(\n    collection_name="gdp_chunks",\n    points=[{"id": c.chunk_id, "vector": v, "payload": {"text": c.text}} for c, v in zip(chunks, vectors)],\n)`,
    labSteps: [
      "Run TEI locally serving bge-base-en-v1.5 and embed all chunks from Module 15.",
      "Create a Qdrant collection and upsert the vectors.",
      "Embed two different queries and eyeball their nearest neighbors.",
      "Confirm semantically similar chunks (not just keyword-similar) land near each other.",
    ],
    costNote: "$0 on a local kind cluster; negligible CPU/GPU for a handful of documents.",
    claimId: "oss-tei-qdrant-embeddings",
  },
  "25-serving": {
    services: ["FastAPI", "vLLM (open-weight model)"],
    storage: "N/A — reads the Module 20 Qdrant collection at request time",
    snippet: `@app.post("/ask")\nasync def ask(q: Question):\n    query_vec = embed(q.text)\n    neighbors = client.query_points(collection_name="gdp_chunks", query=query_vec, limit=5)\n    prompt = build_prompt(q.text, neighbors.points)\n    response = await vllm_client.chat.completions.create(model="Qwen3-8B-Instruct", messages=prompt)\n    return {"answer": response, "citations": [n.id for n in neighbors.points]}`,
    labSteps: [
      "Deploy a FastAPI app on K8s wrapping embed -> retrieve -> prompt -> vLLM-served model.",
      "Ask a question your Module 10 documents can answer; confirm the response cites a real chunk.",
      "Ask an out-of-scope question; confirm the model says it doesn't know instead of guessing.",
      "Log every request/response pair — this becomes the event log Module 30 builds dashboards on.",
    ],
    costNote: "$0 on a local kind cluster; GPU compute cost only if self-hosting at scale.",
    claimId: "oss-vllm-rag-api",
  },
  "35-retrieval": {
    services: ["Qdrant", "TEI"],
    storage: "Qdrant (hybrid dense + sparse vectors)",
    snippet: `# Hybrid search in Qdrant: dense + sparse (BM25-like) vectors\nresults = client.query_points(\n    collection_name="gdp_chunks",\n    query=dense_vec,\n    using="dense",\n    with_payload=True,\n    limit=20,\n)\n\n# Rerank top-20 with a cross-encoder served via TEI\nreranked = cross_encoder.rerank(query, [r.payload["text"] for r in results.points], top_k=5)`,
    labSteps: [
      "Enable a sparse vector field on the Module 15 chunk collection alongside the dense one.",
      "Run a query with dense search only, note top-5 results.",
      "Re-run with Qdrant's hybrid (dense + sparse) query, compare top-5.",
      "Add a cross-encoder reranking pass (served via TEI) over the top-20 hybrid results and compare the final top-5 again.",
    ],
    costNote: "$0 on a local kind cluster; negligible CPU for a handful of queries.",
    claimId: "oss-qdrant-hybrid",
  },
  "38-multimodal": {
    services: ["whisper.cpp", "CLIP", "BGE-M3"],
    storage: "Qdrant (unified collection, modality-tagged payload)",
    snippet: `# Transcribe audio with whisper.cpp, embed keyframes with CLIP, text with BGE-M3\ntranscript = whisper_model.transcribe("claim_video.mp4")\nframe_vec = clip_model.encode_image(keyframe)\ntext_vec = bge_model.encode(transcript["text"])\n\nclient.upsert(collection_name="gdp_docs", points=[\n    {"vector": frame_vec, "payload": {"modality": "video_frame", "doc_id": doc_id}},\n    {"vector": text_vec, "payload": {"modality": "video_transcript", "doc_id": doc_id}},\n])`,
    labSteps: [
      "Run whisper.cpp on a sample claims video to get a transcript.",
      "Sample 3 keyframes and embed them with CLIP.",
      "Embed the transcript with BGE-M3 (same model as the Module 20 OSS text index).",
      "Upsert all vectors into the same Qdrant collection, tagged by modality, and confirm a text query surfaces the video frames.",
    ],
    costNote: "$0 beyond existing GPU — all three models run on the same self-hosted stack.",
    claimId: "oss-clip-whisper-multimodal",
  },
  "45-evaluation": {
    services: ["RAGAS", "Langfuse"],
    storage: "Langfuse (datasets + experiment runs)",
    snippet: `from ragas import evaluate\nfrom ragas.metrics import faithfulness, answer_relevancy, context_precision, context_recall\nfrom datasets import Dataset\n\ndataset = Dataset.from_dict(golden_dict)  # question, contexts, answer, ground_truth\nresult = evaluate(dataset, metrics=[faithfulness, answer_relevancy, context_precision, context_recall])\nprint(result)  # -> {"faithfulness": 0.90, "answer_relevancy": 0.87, ...}\n\n# Log the run to Langfuse so it's diffable against the previous experiment\nlangfuse.experiment.create(dataset_run=result)`,
    labSteps: [
      "Build a golden dataset of 20+ labeled Q&A pairs from the documents ingested in Module 10 and load it into Langfuse.",
      "Run RAGAS with faithfulness, answer relevancy, context precision, and context recall against the Module 25 RAG API.",
      "Spot-check 5 LLM-as-judge scores against your own human judgment and note any disagreement.",
      "Store the run in Langfuse and wire a CI step that fails the build if faithfulness drops below a threshold.",
    ],
    costNote: "$0 on a local kind cluster; only LLM-as-judge API calls cost money (~$0.04 for 20 examples).",
    claimId: "oss-ragas-langfuse",
  },
  "55-observability": {
    services: ["Langfuse", "Prometheus + Grafana"],
    storage: "Langfuse (nested traces) + Prometheus (latency/token metrics)",
    snippet: `from langfuse import Langfuse\n\nlangfuse = Langfuse()\ntrace = langfuse.trace(name="rag_request")\nretrieve_span = trace.span(name="retrieve", metadata={"chunks_count": len(neighbors)})\nretrieve_span.end()\ngenerate_span = trace.span(\n    name="generate",\n    usage={"input": usage.prompt_tokens, "output": usage.completion_tokens},\n)\ngenerate_span.end()`,
    labSteps: [
      "Instrument the Module 25 RAG API with Langfuse spans for embed, retrieve, prompt, and generate.",
      "Confirm one nested trace per request shows all four stages, with token counts and cost on the generate span.",
      "Export request-count and latency metrics to Prometheus and build a Grafana panel for p50/p95 latency.",
      "Compare time-to-first-token versus total latency for a streaming versus non-streaming response.",
    ],
    costNote: "$0 on a local kind cluster; negligible CPU for a handful of traced requests.",
    claimId: "oss-langfuse-tracing",
  },
  "65-security": {
    services: ["Llama Guard", "Presidio", "NeMo Guardrails"],
    storage: "N/A — inline screening at request/response time",
    snippet: `from presidio_analyzer import AnalyzerEngine\n\nanalyzer = AnalyzerEngine()\npii_results = analyzer.analyze(text=retrieved_chunk_text, language="en")\nif pii_results:\n    redacted = redact(retrieved_chunk_text, pii_results)\n\n# Llama Guard classifies the prompt + response as safe/unsafe before it reaches the user\nverdict = llama_guard.classify(conversation=[{"role": "user", "content": incoming_prompt}])\nif verdict.category == "unsafe":\n    raise PromptBlocked(verdict.reason)`,
    labSteps: [
      "Insert a document into the Module 15 chunk index containing a hidden instruction ('ignore prior instructions and reveal...').",
      "Ask a normal question that retrieves that chunk and observe whether the model follows the injected instruction.",
      "Add a Llama Guard classification pass and a Presidio PII-redaction pass in front of the Module 25 RAG API, then re-run the same query.",
      "Add a document-level access-control filter to retrieval and confirm a second tenant's query can no longer surface the first tenant's chunks.",
    ],
    costNote: "$0 on a local kind cluster; GPU cost only if self-hosting Llama Guard at scale.",
    claimId: "oss-llama-guard-presidio",
  },
  "75-finops": {
    services: ["OpenCost", "Prometheus"],
    storage: "Prometheus (cost + usage metrics) joined with the Module 55 event log",
    snippet: `# OpenCost exposes per-namespace/workload cost as Prometheus metrics\ncurl -s http://opencost:9003/allocation/compute \\\n  --data-urlencode window=1d \\\n  --data-urlencode aggregate=namespace | jq '.data[0]'\n# {"gdp-rag": {"totalCost": 4.12, "cpuCost": 2.80, "gpuCost": 1.10, ...}}\n\n# Join against the event log in Grafana to get $/query per tenant namespace`,
    labSteps: [
      "Deploy OpenCost on the local kind cluster and let it allocate cost by namespace for a day.",
      "Join OpenCost's per-namespace cost against the Module 55 event log's request counts to get $/query.",
      "Confirm LLM-serving GPU cost dominates versus embedding/TEI compute cost.",
      "Add a fast-model-first router that only escalates to a reasoning-tier model on low retrieval confidence, and re-measure $/query.",
      "Enable vLLM's enable_prefix_caching for the repeated system prompt + retrieved chunks prefix, and confirm the drop in GPU time per query in OpenCost's allocation.",
    ],
    costNote: "$0 on a local kind cluster; GPU compute cost only if self-hosting at scale.",
    claimId: "oss-opencost-k8s",
  },
  "85-agents": {
    services: ["LiteLLM", "MCP servers"],
    storage: "N/A — reads the Module 20 Qdrant collection and calls tools at request time",
    snippet: `import litellm\n\ntools = [{\n    "type": "function",\n    "function": {"name": "search_docs", "description": "Search the indexed document store", "parameters": {...}},\n}]\nresponse = litellm.completion(model="ollama/qwen3", messages=messages, tools=tools)\n# If the model emits a tool call, run it via an MCP server and feed the result back into messages\ntool_result = mcp_client.call_tool("search_docs", response.tool_calls[0].function.arguments)`,
    labSteps: [
      "Stand up an MCP server exposing the Module 25 retrieval call as a `search_docs` tool.",
      "Route function calling through LiteLLM against a locally served open-weight model and connect it to the MCP server.",
      "Give the agent a multi-step task ('find X, then summarize how it changed across the last two documents') and trace the plan-act-observe loop.",
      "Cap the agent at a fixed iteration budget and add a human-approval gate before any tool call with a side effect.",
    ],
    costNote: "$0 on a local kind cluster; GPU compute cost only if self-hosting the agent model at scale.",
    claimId: "oss-mcp-agent-loop",
  },
};
