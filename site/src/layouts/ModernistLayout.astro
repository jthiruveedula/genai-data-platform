---
/**
 * Shell for the Modernist-bound scroll page.
 *
 * Deliberately does not extend BaseLayout: this page carries its own nav,
 * its own token sheet, and a full-bleed ground, so the site chrome (navbar,
 * atmosphere, reading progress, cursor) would fight it. Head metadata
 * follows the same conventions BaseLayout uses.
 */
import "@fontsource/archivo/400.css";
import "@fontsource/archivo/500.css";
import "@fontsource/archivo/600.css";
import "@fontsource/archivo/700.css";
import "@fontsource/archivo/800.css";
import archivo400 from "@fontsource/archivo/files/archivo-latin-400-normal.woff2?url";
import archivo800 from "@fontsource/archivo/files/archivo-latin-800-normal.woff2?url";
import "../styles/modernist.css";
import "../styles/platform.css";

interface Props {
  title: string;
  description: string;
  /**
   * Cloud whose accent the page opens on — see the accent block in platform.css.
   * `"neutral"` emits no `data-pf-cloud` at all, leaving the base Modernist
   * vermilion in place: for pages whose artwork is baked in one accent and so
   * cannot follow a cloud switch (e.g. `/world/`, whose film is pre-rendered).
   */
  cloud?: "gcp" | "aws" | "azure" | "oss" | "neutral";
}

const { title, description, cloud = "gcp" } = Astro.props;
---

<!doctype html>
<html lang="en" data-pf-cloud={cloud === "neutral" ? undefined : cloud}>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="description" content={description} />
    <link rel="canonical" href={new URL(Astro.url.pathname, Astro.site)} />
    <meta property="og:type" content="website" />
    <meta property="og:site_name" content="GenAI Data Platform" />
    <meta property="og:title" content={`${title} · GenAI Data Platform`} />
    <meta property="og:description" content={description} />
    <meta property="og:url" content={new URL(Astro.url.pathname, Astro.site)} />
    <meta name="twitter:card" content="summary" />
    <meta name="twitter:title" content={`${title} · GenAI Data Platform`} />
    <meta name="twitter:description" content={description} />
    <link rel="icon" type="image/svg+xml" href={`${import.meta.env.BASE_URL}favicon.svg`} />
    <link rel="preload" href={archivo400} as="font" type="font/woff2" crossorigin />
    <link rel="preload" href={archivo800} as="font" type="font/woff2" crossorigin />
    <title>{title} · GenAI Data Platform</title>
    <slot name="head" />
  </head>
  <body>
    <slot />
  </body>
</html>
